import { LanguageSwitcher } from "../components/features";

const Settings = () => {
    return (
        <div>
            <LanguageSwitcher />
        </div>
    );
};

export default Settings;