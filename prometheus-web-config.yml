# Prometheus Web Configuration for HTTPS/TLS
# This enables HTTPS on the Prometheus web interface

tls_server_config:
  cert_file: /etc/prometheus/certs/prometheus.crt
  key_file: /etc/prometheus/certs/prometheus.key
